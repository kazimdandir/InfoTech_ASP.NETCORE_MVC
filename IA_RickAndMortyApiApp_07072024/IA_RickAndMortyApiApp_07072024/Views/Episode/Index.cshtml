@* @model IEnumerable<Result>

@{
    ViewData["Title"] = "Index";
}

<h2>Episodes</h2>
<hr />

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<table class="table table-hover table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Air Date</th>
            <th>Episode</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.name</td>
                <td>@item.air_date</td>
                <td>@item.episode</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.id" class="btn btn-success">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.id" class="btn btn-warning">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.id" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table> *@

@model IEnumerable<Result>

@{
    ViewData["Title"] = "Index";
}

<h2>Episodes</h2>
<hr />

<div class="row">
    @foreach (var episode in Model)
    {
        <div class="col-md-4">
            <div class="card mb-4">
                <div class="card-body">
                    <h5 class="card-title">@episode.name</h5>
                    <h6 class="card-subtitle mb-2 text-muted">@episode.air_date</h6>
                    <p class="card-text">Episode: @episode.episode</p>
                    @* <a asp-action="Edit" asp-route-id="@episode.id" class="card-link btn btn-success">Edit</a> *@
                    <a asp-action="Details" asp-route-id="@episode.id" class="card-link btn btn-warning">Details</a>
                    @* <a asp-action="Delete" asp-route-id="@episode.id" class="card-link btn btn-danger">Delete</a> *@
                </div>
            </div>
        </div>
    }
</div>

